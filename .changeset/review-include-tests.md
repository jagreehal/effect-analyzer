---
'effect-analyzer': patch
---

Add `--include-tests` to `effect-analyze review`, and a matching `include-tests` input on the GitHub Action, so projects whose Effect programs live in `*.test.ts` / `*.spec.ts` files can review them too.
